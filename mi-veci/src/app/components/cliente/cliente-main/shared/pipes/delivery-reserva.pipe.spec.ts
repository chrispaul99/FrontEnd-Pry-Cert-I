import { DeliveryReservaPipe } from './delivery-reserva.pipe';

describe('DeliveryReservaPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryReservaPipe();
    expect(pipe).toBeTruthy();
  });
});
